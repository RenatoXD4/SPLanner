import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  imports: [],
  templateUrl: './login.html',
  styleUrl: './login.css'
})
export class Login {

  //Esta parte es solo la capa de lógica de la UI, solo llamará al loginService con sus funciones y todos los datos se mostrarán en el html
}
